version: 0.0
os: linux
hooks:
  BeforeInstall:
    - location: artifacts/scripts/pull_image.sh
      timeout: 300
      runas: root
  ApplicationStart:
    - location: artifacts/scripts/start_service.sh
      timeout: 300
      runas: root